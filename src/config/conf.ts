/**
 * @class Conf
 * @property {string} name Property of config.yml
 */
class Conf {
    public readonly name: string;

    constructor(name: string) {
        this.name = name;
    }
}

export default Conf;
